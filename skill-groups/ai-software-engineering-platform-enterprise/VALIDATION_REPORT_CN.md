# 智能软件工程平台 5.11 桌面端验证报告

生成版本：`5.11.0+codex.20260813232721`

验证日期：2026-08-13

## 结论

- 发布包、源码结构、行为回归与五插件全量 Skill 一致性审核全部通过。
- 本机五个插件的仓库源码、个人安装目录、5.11 版本缓存三层哈希全部一致，启用配置和全局路由规则一致。
- 运行时热切换复验未通过：在安装后新建的桌面任务中，实际加载路径仍指向 `5.10.0+codex.20260813053908`。因此不能声称本次已在当前进程热切换生效；必须重启桌面应用，再新建任务复验实际路径。

## 发布级审核

- 插件：5/5。
- 原子 Skill：42/42。
- 唯一隐式入口：智能工程轻量路由；其余41个能力按需读取或手动选择。
- 一致性检查：结构、路由、归属、权限、可用性、性能均为0错误、0警告。
- 路由行为 Eval：正向94/94，负向38/38。
- 单元回归：核心34、工作区29、质量22，共85/85通过。

## 5.11 新增门禁

- 动作优先阶段路由，避免“增强后审核并推送”被尾句抢走当前开发阶段。
- Skill 加载遥测回执，候选命中不等于已经应用。
- 桌面任务查询错误、超时与明确空结果分离，只有明确空结果允许创建。
- 项目环境预检与状态告警指纹去重。
- 不可变 `candidate_id` 绑定 Review、Testing 与 Merge；源码或工作区变化立即使旧证据过期。
- 根因假设账本，同一问题连续两个假设被否定后进入 `DIAGNOSIS_REQUIRED`。
- 大测试矩阵先做运行时、目录、命令长度、计数、变异及正反样本预检。
- 跨会话/跨 Agent 交接阻断原始密码、令牌、私钥和带凭据 URL。
- Windows Git 子进程统一使用 UTF-8 容错解码。

## 安装核验

- 安装目录：`C:\Users\Administrator\.codex\plugins\<插件>`。
- 5.11 缓存：`C:\Users\Administrator\.codex\plugins\cache\personal-ai-engineering-marketplace\<插件>\5.11.0+codex.20260813232721`。
- 五插件启用：5/5，方式为 `desktop-config`。
- 三层哈希：5/5一致，`mismatches=[]`。
- 全局自动路由规则：一致。
- 安装备份：`C:\Users\Administrator\.codex\plugins-backup\20260813T153254Z`。

## 热切换实测

- 验证任务：`019ffbc1-a9ec-7d60-b541-9a561682dd1e`。
- 验证方式：安装完成后新建无项目桌面任务，只读检查实际可见的 Skill 与 `plugin.json` 路径。
- 结果：轻量路由可见，但实际路径仍为 `C:\Users\Administrator\.codex\plugins\cache\personal-ai-engineering-marketplace\ai-engineering-core\5.10.0+codex.20260813053908`。
- 判定：当前桌面进程缓存了旧插件注册表；文件级安装成功，运行时激活未成功。
- 下一步：重启 ChatGPT/Codex 桌面应用后，新建任务再次读取路径；必须显示 `5.11.0+codex.20260813232721` 才能判定运行时生效。

## 发布包

- 已生成五个 `5.11.0` ZIP，并移除发布目录中的五个旧 `5.10.0` ZIP。
- `SHA256SUMS.txt` 已更新。
- 生成发布包前与生成后均执行五插件全部 Skill 一致性审核；失败会阻断发布。

## 能力边界

本报告证明插件结构、路由、脚本、发布包、安装文件与配置正确，不证明任意业务项目已经通过真实端到端验收。对具体项目仍须建立 Task、变更契约、候选版本与对应层级的运行证据。
