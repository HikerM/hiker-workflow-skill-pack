# 智能软件工程平台 5.10 桌面端验证报告

生成版本：`5.10.0+codex.20260813053908`

生成日期：2026-08-13

## 结论

- 套件包含5个插件、42个原子 Skill；仅“智能工程轻量路由”允许隐式调用，其余41个只由路由按需读取或用户手动选择。
- 发布级一致性审计逐一覆盖42个 Skill，结构、路由、归属、权限、可用性和性能六类检查均为0错误、0警告。
- 本机已按 ChatGPT/Codex 桌面端目录重新安装；五个插件的仓库源码、安装目录和版本缓存哈希5/5一致，均已启用，全局规则一致。

## 本次增强验证

- 超长单会话与多轮上下文压缩使用 `.ai/runtime/skill-routing.json` 保存阶段、最多两个活跃原子 Skill、待执行名称和路由指纹，不保存 Skill 正文。
- 会话第一条助手输出先显示“智能工程轻量路由”；确定性路由后显示实际活跃原子 Skill。轻量路由不占两个原子 Skill 额度。
- 第三个及之后的相关 Skill 进入有界待执行队列，当前门禁完成后重新路由，测试确认不会像旧逻辑一样静默丢失。
- `.ai` 使用热状态、有界索引和压缩冷归档；旧 Checkpoint 在退出热目录前打包为 ZIP，账本记录归档路径和哈希，可按名称恢复。
- 日常任务创建、状态渲染和 Checkpoint 优先读取 `task-index.json` 的活动 Task，不遍历全部历史 Task；冷归档不参与启动或普通路由扫描。

## 结构、冲突与路由

- 插件清单验证：5/5通过。
- Skill快速验证：新增 Skill 通过frontmatter、目录和元数据验证。
- 正向路由 Eval：94/94，通过率100%。
- 负向路由 Eval：38/38，特异度100%。
- 架构请求会加载“架构决策挑战与补全”；普通“解释B/S架构”不会误触发。
- 所有用户可见插件名和 Skill 名均为中文；应用回执只显示实际使用项，不显示组织分类、项目、模式、原因、未使用能力或英文内部ID。
- 五插件全部 Skill 的登记、中文名称、职责类型、源码修改权限、默认提示、路由覆盖、正向Eval、本地引用、触发描述重叠和正文体积均通过。

## 功能与回归

- 单元测试：核心30、浏览器端与服务端10、客户端7、工作区24、质量20，共91/91通过。
- 集成 Smoke：19/19通过，覆盖全套Skill一致性、初始化、压缩恢复、有界记忆、长链路收敛、受治理Worktree、完整变更集、风险标记、图谱限流、真实命令、个人安装、桌面启用、三层哈希、缓存保留、全局规则幂等和安全卸载。
- 发布验证器：插件5、Skill 42，0错误、0警告。
- 新增长会话、路由额度、待执行队列、路由态 Checkpoint 与冷归档恢复包回归测试，全部通过。

## 性能

- 轻量路由仍是唯一隐式入口；它不占额度，每阶段最多读取两个活跃原子 Skill，后续能力只保留名称并延迟加载。全套一致性审计仅在插件增强或发布时运行，不进入普通项目请求。
- 20次本地冷进程路由：中位数117.11ms、P95 179.28ms，低于500ms门禁。该指标不包含桌面端网络和模型首字延迟。
- 热区最多保留12个近期和8个里程碑 Checkpoint；旧恢复包进入压缩冷归档。普通会话不扫描 `.ai/archive/` 或全部 Task。
- `convergence_guard.py` 的事件、实验、验收证据和验证记录均有固定上限；验证证据按指纹复用，不随长会话无界增长。
- 普通局部任务不会启动多会话控制账本、跨运行时对抗矩阵或全套插件扫描。

## 桌面端本机安装

- 安装路径：`C:\Users\Administrator\.codex\plugins\<插件>`。
- 缓存路径：`C:\Users\Administrator\.codex\plugins\cache\personal-ai-engineering-marketplace\<插件>\5.10.0+codex.20260813053908`。
- 插件启用：5/5为 `enabled = true`，激活方式为 `desktop-config`。
- 三层哈希：源码、安装目录、版本缓存5/5完全一致，`mismatches=[]`。
- 全局规则：`C:\Users\Administrator\.codex\AGENTS.md` 与仓库模板一致，只自动进入“智能工程轻量路由”。
- 安装状态：`C:\Users\Administrator\.codex\plugin-install-state.json`，`ok=true`。
- 本次插件备份：`C:\Users\Administrator\.codex\plugins-backup\20260813T054153Z`。
- Marketplace备份：`C:\Users\Administrator\.agents\plugins\marketplace.json.20260813T054153Z.bak`。
- 新建桌面任务即可加载新版本；只有新任务仍显示旧缓存时才需要重启桌面应用。

## 分发包

- 已生成五个 `5.10.0` ZIP，并清理五个旧 `5.9.0` ZIP；发布目录只保留当前版本。
- `SHA256SUMS.txt` 已重新生成；ZIP排除 `__pycache__`、`.pyc`、`.pyo` 和测试缓存。
- 发布包生成前强制执行五插件全部Skill一致性审计，发现冲突、遗漏、职责曲解或权限越界即阻止打包。

## 边界

本报告验证的是 Skill/插件结构、路由、治理脚本、安装状态和桌面端本地文件快照。具体项目仍需使用该项目真实构建命令、数据库、容器、浏览器、设备环境和发布流水线完成项目级验收。
