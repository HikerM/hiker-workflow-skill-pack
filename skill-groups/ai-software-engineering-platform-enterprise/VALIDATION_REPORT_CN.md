# 企业版 5.4 桌面端验证报告

生成版本：`5.4.0+codex.20260811173000`

生成日期：2026-08-11

## 结论

- 第二组包含 5 个插件、33 个原子 Skill；仅“智能工程轻量路由”允许隐式调用，其余 32 个按路由或手动选择懒加载。
- 第一组与第三组不在自动应用规则中，只有用户明确点名或手动选择时使用。
- 所有插件显示名与 33 个 Skill 显示名均为中文；应用回执要求只显示插件中文名称和 Skill 中文名称，不显示英文内部标识。
- 本机已按 ChatGPT/Codex 桌面端目录完成安装、版本化缓存和启用配置，不依赖 CLI 作为运行入口。

## 结构与路由

- 自定义验证器：通过，插件 5 个、Skill 33 个。
- 正向路由 Eval：74/74，通过率 100%。
- 负向路由 Eval：31/31，特异度 100%。
- 官方插件验证：5/5 通过。
- 官方 Skill 快速验证：33/33 通过；Windows 环境以 UTF-8 模式运行。
- 用户可见名称校验：插件名和 Skill 名均不含英文字母。
- 业务域污染扫描：0；并保留阻断无关领域样例进入第二组的验证规则。

## 功能与回归

- 单元测试：核心 17、质量 14、客户端 7、浏览器端 6、工作区 12，共 56/56 通过。
- 集成 Smoke：16/16 通过，覆盖初始化、上下文压缩恢复、有界记忆、受治理 Worktree、完整变更集、风险标记、图谱限流、真实命令、个人安装、桌面配置启用回退、全局规则幂等与安全卸载。
- 从零开发：先建立稳定需求 ID、冲突、未知项、验收条件与关键技术 Checkpoint，不直接套脚手架。
- 部分源码：先形成带源码/测试证据的能力基线，再把自定义需求对账为新增、修改、替换或移除。
- B/S 与 C/S：从真实工程证据识别前端、客户端、后端、语言、框架、SDK、构建工具和版本；证据不足标记 unknown，不写死 Unity 或某一种客户端方案。
- 多会话：只注入有界当前工作集；完整需求、决定、Task、Git 和验收证据保存在 `.ai` 事实源与检查点中。

## 大型工程保护

- 每个开发 Task 在进入 Development 前必须具有变更契约：允许文件/模块、原有行为不变量、必要测试、消费者和最大影响半径。
- 架构守卫检查范围漂移、公共表面指纹、消费者回归、依赖方向、受保护模块、文件体积与单次增长。
- 默认警告线为 400 行、阻断线为 700 行；单次增长默认 80 行警告、200 行阻断，可按项目调整。
- 架构证据绑定 Git HEAD 与工作区指纹；代码变化后旧证据失效，不能拿旧测试结果通过 Review、合并或发布门禁。
- 采用零配置可运行、约定优先、渐进增强；普通改动不要求维护与源码重复的庞大全量配置，高风险边界才要求显式登记。

## 性能

- 路由只执行深度 3、最多 160 个目录的浅层标记检查，不递归扫描完整源码仓库。
- 当前大型仓库 500 次进程内路由平均 13.799ms，P95 18.915ms；30 次独立 Python 进程平均 89.411ms，P95 115.606ms。
- 每轮最多选择两个原子 Skill；应用回执作为元信息不占混合工作中的功能槽位。
- “大型工程多智能体总控”已改为辅助审核读取 0 份参考、普通治理读取 0–1 份、真正跨治理域最多 2 份，禁止启动时预读全部四份治理文档。

## 桌面端本机安装

- 安装路径：`C:\Users\Administrator\.codex\plugins\<插件>`。
- 缓存路径：`C:\Users\Administrator\.codex\plugins\cache\personal-ai-engineering-marketplace\<插件>\5.4.0+codex.20260811173000`。
- 五个插件的仓库源码→桌面插件目录逐文件差异：全部 0。
- 五个插件的仓库源码→桌面版本化缓存逐文件差异：全部 0。
- `config.toml` 中五个插件各有且只有一个启用段，均为 `enabled = true`。
- `~/.codex/AGENTS.md` 中托管区块只有一份；仅第二组自动，第一组与第三组手动；回执只显示中文插件名和中文 Skill 名。
- 最近一次插件备份：`C:\Users\Administrator\.codex\plugins-backup\20260811T094019Z`；最近一次全局规则变更备份：`C:\Users\Administrator\.codex\agents-backup\20260811T093529Z\AGENTS.md`。
- 当前已打开任务不会热替换其 Skill 快照；安装后优先新建桌面任务。只有新任务仍显示旧版本时才需要重启桌面应用。

## 分发包

- 已生成五个 `5.4.0` ZIP，排除 `__pycache__`、`.pyc` 和 `.pyo`。
- `SHA256SUMS.txt` 已与最终 ZIP 重新计算并保持一致。
- 个人安装器默认直接完成桌面插件复制、版本化缓存、Marketplace 合并、全局路由规则合并和桌面端启用配置；CLI 仅保留为显式旧版兼容选项。

## 边界

本报告验证的是 Skill/插件结构、路由、治理脚本、安装状态和桌面端本地文件快照。具体项目仍需使用该项目真实构建命令、Unity Editor、设备环境、数据库和发布流水线完成项目级验收。
