# 安全说明

- 所有脚本默认只访问当前项目、插件目录或 Git 公共目录。
- 不发送网络请求，不收集遥测，不读取凭据目录。
- 状态与事件脚本输出不包含源码全文、密钥或完整终端日志。
- Worktree 删除、分支删除和回滚均默认拒绝脏状态；`--force` 也要求显式命令。
- 质量脚本不会自动执行测试命令，只生成计划；执行由用户或 Codex 在现有权限模型下完成。
- 状态与事件脚本不是完整安全边界，仍应使用 Codex 沙箱、最小权限和代码审核。
