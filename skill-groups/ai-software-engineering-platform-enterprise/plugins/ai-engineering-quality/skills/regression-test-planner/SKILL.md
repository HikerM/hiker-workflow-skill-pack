---
name: regression-test-planner
description: 当代码、契约、数据、界面、配置或发布范围变化后，依据完整变更风险、受影响消费者和项目真实可执行脚本生成最低必要回归范围、人工验证与证据缺口；不得虚构测试命令、把计划当结果或用单一构建替代功能回归。
---


# 回归测试范围规划

## 职责

读取最新风险报告和当前项目配置，生成：

- 必须执行；
- 建议执行；
- 人工验证；
- 未映射缺口；
- 所需证据。

命令只能来自项目真实 `package.json`、测试配置、解决方案文件或项目策略。若无法发现可执行命令，应明确标记“待配置”，不得虚构命令。

执行大规模测试矩阵前必须运行 `harness_preflight.py`：校验工作目录、运行时、Windows 命令长度、临时目录读写、用例组唯一性与计数、变异证据及最小正反样本。预检为 `INVALID` 时只修测试基础设施并重试预检，不得把工具错误记成产品 `FAIL`，也不得继续启动全量矩阵。

## 输出

- `.ai/evidence/test-plan/latest.json`
- `.ai/evidence/test-plan/latest.md`
